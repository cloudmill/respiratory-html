mixin tab(props)
  .tab&attributes(attributes)
    .tab__button(class=props.buttonClasses)
      .tab__bgs
        .tab__bg.tab__bg--default
        .tab__bg.tab__bg--yellow
      .tab__text
        block
