doctype html

block variables

include ./svg/hub
include ./ui/hub

html.html(lang="ru")
  head
    meta(charset="utf-8")
    title= title || data.title
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body.body
    block header
      |header
    block content
    block footer
      |footer
