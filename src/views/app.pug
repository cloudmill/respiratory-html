doctype html

include ./logic/index

- var appProps = {}
block appProps

include ./ui/index

html.html.no-transition(lang="ru")
  head
    meta(charset="utf-8")
    title= appProps.title || data.title
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body.body
    block header
      +header
    .wrapper
      block content
        |content
      block footer
        +footer
    block preloader
      +preloader
    block grid-test
      +grid-test
