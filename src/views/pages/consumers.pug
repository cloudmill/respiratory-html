extends ../app

block definitions
  -
    app.title = 'Потребителям';

block header
  +header({ isTransparent: false })
//- block footer

block preloader

block grid-test
block test

block content
  +space(['header'])
  +space(['sub-block'])
  .container
    +breadcrumbs({ items: ['Главная', 'работа у нас'], breadcrumbMods: ['dark'] })
  +space(['sub-block'])
  +heading(1, ['h1'])
  +space(['block'])
  div
    |123